apiVersion: v1
kind: Service
metadata:
  name: pi-dashboard-nodeport
  namespace: pi-dashboard
spec:
  type: NodePort
  selector:
    app: pi-dashboard
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080