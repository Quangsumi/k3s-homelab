apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pi-dashboard
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Quangsumi/k3s-homelab.git
    targetRevision: main
    path: apps/pi-dashboard

  destination:
    server: https://kubernetes.default.svc
    namespace: pi-dashboard

  syncPolicy:
    syncOptions:
      - CreateNamespace=true