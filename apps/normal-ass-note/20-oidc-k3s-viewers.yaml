apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: oidc-k3s-viewers
  namespace: normal-ass-note
subjects:
  - kind: Group             # bind to a group
    name: oidc:k3s-viewers  # group name in the OIDC provider
    apiGroup: rbac.authorization.k8s.io
# subjects:
#   - kind: User            # bind to a user
#     name: oidc:onlyview   # user name in the OIDC provider
#     apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: ClusterRole
  name: view
  apiGroup: rbac.authorization.k8s.io