apiVersion: v1
kind: Namespace
metadata:
  name: litellm

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: litellm-credentials
  namespace: litellm
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  target:
    name: litellm-secret-infisical
    creationPolicy: Owner
  data:
    - secretKey: LITELLM_MASTER_KEY
      remoteRef:
        key: /litellm/LITELLM_MASTER_KEY
    - secretKey: LITELLM_SALT_KEY
      remoteRef:
        key: /litellm/LITELLM_SALT_KEY
    - secretKey: DATABASE_URL
      remoteRef:
        key: /litellm/DATABASE_URL
    - secretKey: UI_USERNAME
      remoteRef:
        key: /litellm/UI_USERNAME
    - secretKey: UI_PASSWORD
      remoteRef:
        key: /litellm/UI_PASSWORD

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-config
  namespace: litellm
data:
  config.yaml: |
    general_settings:
      database_connection_pool_limit: 5
      proxy_batch_write_at: 60
      allow_requests_on_db_unavailable: false

    litellm_settings:
      callbacks: ["prometheus"]
      service_callback: ["prometheus_system"]
      request_timeout: 600
      set_verbose: false
      json_logs: true

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: litellm
  namespace: litellm
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: litellm
  template:
    metadata:
      labels:
        app: litellm
    spec:
      containers:
        - name: litellm
          image: ghcr.io/berriai/litellm-database:1.86.0
          imagePullPolicy: IfNotPresent
          args:
            - "--host"
            - "0.0.0.0"
            - "--port"
            - "4000"
            - "--config"
            - "/app/config.yaml"
            - "--num_workers"
            - "1"
          ports:
            - name: http
              containerPort: 4000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: litellm-secret-infisical
                  key: DATABASE_URL
            - name: LITELLM_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: litellm-secret-infisical
                  key: LITELLM_MASTER_KEY
            - name: LITELLM_SALT_KEY
              valueFrom:
                secretKeyRef:
                  name: litellm-secret-infisical
                  key: LITELLM_SALT_KEY
            - name: LITELLM_MODE
              value: "PRODUCTION"
            - name: USE_PRISMA_MIGRATE
              value: "True"
            - name: LITELLM_LOG
              value: "INFO"
            - name: STORE_MODEL_IN_DB
              value: "True"
          
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 12
          
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 180
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 12
          
          startupProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 180 # prisma migration might take too long
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 120
          
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              memory: 2Gi
          volumeMounts:
            - name: litellm-config
              mountPath: /app/config.yaml
              subPath: config.yaml
              readOnly: true
      volumes:
        - name: litellm-config
          configMap:
            name: litellm-config
---
apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: litellm
spec:
  type: ClusterIP
  selector:
    app: litellm
  ports:
    - name: http
      port: 4000
      targetPort: http

# ---
# apiVersion: v1
# kind: Service 
# metadata:
#   name: litellm-lb
#   namespace: litellm
#   annotations:
#     metallb.io/loadBalancerIPs: 192.168.63.219
# spec:
#   type: LoadBalancer
#   selector:
#     app: litellm
#   ports:
#     - name: http
#       port: 80
#       targetPort: http

# ---
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: litellm-tailscale
#   namespace: litellm
#   #annotations:
#   #  tailscale.com/funnel: "true"
# spec:
#   ingressClassName: tailscale
#   defaultBackend:
#     service:
#       name: litellm
#       port:
#         number: 4000
#   tls:
#     - hosts:
#         - litellm

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: litellm
  namespace: litellm
  annotations:
    cert-manager.io/cluster-issuer: homelab-ca
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - litellm.lab
      secretName: litellm-lab-tls
  rules:
    - host: litellm.lab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: litellm
                port:
                  number: 4000